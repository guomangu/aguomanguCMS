{% extends 'base.html.twig' %}

{% block content %}
    <h1>Créer un article</h1>

    {{ form_start(form) }}
        {{ form_row(form.titre) }}
        {{ form_row(form.image) }}

        <button type="submit">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}


